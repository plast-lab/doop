This directory contains a test program that uses Doop as a library.

Quick setup:

1. Build the example program:
```
gradle build
```

2. Set DOOP_HOME to doop-home directory (see message printed by previous step).

3. Run the example program via Gradle:
```
gradle run
```
