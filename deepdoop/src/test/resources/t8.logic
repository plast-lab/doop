
P(x) -> string(x).
Q(x, y) -> string(x), int(y).

Q(x, y) <- P(x), y = 10.

.comp Foo {
	P1(x) <- P(x).
	P1(x) -> string(x).
}

.cmd Bar {
	lang:cmd:DIR[] = "../dir1".
	lang:cmd:IN(`P1).
	lang:cmd:OUT(`A).
	lang:cmd:OUT(`B).
	lang:cmd:RUN[] = "dir2/run.sh -flag1".
	A(x, y) -> string(x), string(y).
	B(x) -> int(x).
}

.init S1 = Foo
//.init S2 = Bar

//.propagate S1 { * } to S2
//.propagate S2 { * } to .global
