ServletClass(?type) -> ClassType(?type).

MockObject(?receiverHeap, ?class) ->
   string(?receiverHeap), ClassType(?class).

MockEntryPoint(?class, ?method) ->
   ClassType(?class), Method(?method).

ServletEntryMethodName(?str) -> string(?str).
