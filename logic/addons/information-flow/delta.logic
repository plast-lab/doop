+TaintedHeapAllocation(?invocation),
+HeapAllocation(?invocation),
+HeapAllocation:Type[?invocation] = ?retType <-
  TaintSourceMethod(?tomethod),
  CallGraphEdge(_, ?invocation, _, ?tomethod),
  MethodSignature:ReturnType[?tomethod] = ?retType.

// see gatekeeper paper (ben livshits)