#include "../../core/context-sensitivity-declarations.logic"

// For this analysis, there are no heap contexts, so this is
// what the initial system objects should have.
MySystemThreadGroup(?alloc) -> HeapAllocation(?alloc).

MyMainThreadGroup(?alloc) -> HeapAllocation(?alloc).

MyMainThread(?alloc) -> HeapAllocation(?alloc).
