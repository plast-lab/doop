
.output isFunction
.output isMethod
isFunction(?function):-
    _Function(?function, _, ?par, _),
    isFunction(?par).

isFunction(?function):-
    _Function(?function, _, ?function, _).

isMethod(?function):-
    _Function(?function, _, ?par, _),
    isClassType(?par).

.decl FormalParamName(?var:symbol, ?name:symbol)
.output FormalParamName
FormalParamName(?var, ?name):-
    _FormalParam(_,_,?var),
    VarSourceName(?var,?name).