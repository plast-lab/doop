// For this analysis, contexts are types, so this is
// what the initial system objects should have.
MySystemThreadGroup(?type, ?heap) ->
  Type(?type), HeapAllocation(?heap).

MyMainThreadGroup(?type, ?heap) ->
  Type(?type), HeapAllocation(?heap).

MyMainThread(?type, ?heap) ->
  Type(?type), HeapAllocation(?heap).
