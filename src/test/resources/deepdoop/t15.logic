@Entity Animal.
@Entity AP.

@Entity Cat -> Animal.

@Output
Foo(x, y) -> AP(x), string(y).

Foo(ap, x) <- x = "J", ap = x.

@Output, @Entity Bar.

@Output {
	FooBar(x, y) -> AP(x), string(y).

	@Entity Baz.
}

@Output, @Entity {
	Dog.
	Cat.
}

@Input {
	P1(x) -> string(x).
	P2(x) -> int(x).
	P3(x) -> string(x).
}
