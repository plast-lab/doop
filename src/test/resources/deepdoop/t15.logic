@Entity Animal(x).
@Entity AP(x).

@Constructor
cons:byA[x,y] = a -> string(x), int(y), AP(a).

@Constructor
cons:byB[x,y,z] = a -> string(x), string(y), int(z), AP(a).

Foo(x, y) -> AP(x), string(y).

new<<cons:byA[y,1] = z as AP>>, Foo(z, y) <- y = "J".
