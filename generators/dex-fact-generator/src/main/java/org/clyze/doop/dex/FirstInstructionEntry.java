package org.clyze.doop.dex;

class FirstInstructionEntry {
    final int address;
    final int index;

    FirstInstructionEntry(int address, int index) {
        this.address = address;
        this.index = index;
    }
}
